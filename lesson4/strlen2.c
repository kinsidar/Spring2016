#include <stdio.h>

int stringlength(char string[]);

int main()
{
	char str[] = "The quick brown fox";

	printf ("The length of the string %s is %d\n ",
		str, stringlength(str));
}

int stringlength(char string[])
{
	/*magic and then it returns the lenght of the string*/

}
